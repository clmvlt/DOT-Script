module.exports = class {
    /**
     * 
     * @param {String} guildId 
     * @param {String} prefix 
     */
    constructor (guildId, prefix) {
        this.guildId = guildId;
        this.prefix = prefix;
    }
}